import java.util.ArrayList;
import java.util.List;

public class DisplayWord {
       private String word;
       private List<Character> letters = new ArrayList<Character>();
       private List<Character> guessedLetters = new ArrayList<Character>();

    /**
     *  use stringbuilder to create output string
     *  check each letter, if it is in the guessed letters, display the letter, otherwise display an underscore
     *
     */
}
